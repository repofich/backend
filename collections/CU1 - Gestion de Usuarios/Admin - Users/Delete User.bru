meta {
  name: Delete User
  type: http
  seq: 6
}

delete {
  url: http://localhost:8000/api/users/99
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Authorization: Bearer {{admin_token}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
