meta {
  name: Asignar evaluador - sin token
  type: http
  seq: 11
}

post {
  url: http://localhost:8000/api/thesis/1/evaluator
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "user_id": 5
  }
}

tests {
  test("Status 401", function() {
    expect(res.status).to.equal(401);
  });
}
