meta {
  name: Listar tesis publicadas - filtro status
  type: http
  seq: 23
}

get {
  url: http://localhost:8000/api/thesis/published?status=publicado
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Solo publicadas", function() {
    expect(res.body.data).to.be.an("array");
  });
}
