meta {
  name: Update Thesis Status - Volver a revision
  type: http
  seq: 8
}

put {
  url: http://localhost:8000/api/thesis/1/status
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  Accept: application/json
  Authorization: Bearer {{admin_token}}
}

body:json {
  {
    "status": "revision"
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Success message", function() {
    expect(res.body.message).to.include("actualizado");
  });
  test("Status updated to revision", function() {
    expect(res.body.thesis.status).to.equal("revision");
  });
}
