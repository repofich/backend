meta {
  name: Delete User Curriculum
  type: http
  seq: 10
}

delete {
  url: http://localhost:8000/api/users/6/curriculum
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Authorization: Bearer {{admin_token}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
