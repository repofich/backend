meta {
  name: Delete Career - 404
  type: http
  seq: 13
}

delete {
  url: http://localhost:8000/api/careers/9999
  body: none
  auth: bearer
}

headers {
  Accept: application/json
  Authorization: Bearer {{admin_token}}
}

tests {
  test("Status 404", function() {
    expect(res.status).to.equal(404);
  });
}
