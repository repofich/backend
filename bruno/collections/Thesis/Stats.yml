info:
  name: Stats
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/thesis/featured"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
