info:
  name: Recent
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/thesis/search?title=sistema"
  params:
    - name: title
      value: sistema
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
