meta {
  name: Listar tesis publicadas
  type: http
  seq: 22
}

get {
  url: http://localhost:8000/api/thesis/published
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Lista de tesis", function() {
    expect(res.body.data).to.be.an("array");
  });
}
